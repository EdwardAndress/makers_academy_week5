<title>Shoot Boats Page</title>
	Let's shoot some boats <%=@player.name%>

	<%=@opponent.name%>'s board:
	<table>
		<% @opponent.board.grid.each_slice(10) do |slice|%>
			<tr>
			<% slice.each do |key, v| %>
				<td>
				<% if !v.shot_at? %>
					<form action='/shoot' method="post">
					<input type="hidden" name = "cell_key" value = "<%= key %>">
					<button type="submit"><%= v.status %></button></td></form>
				<% else %>
					<%= v.status %>
				<% end %>
			</td>
		<%end%>
		</tr>
	<%end%> 
</table>

Your board
<table>
		<% @player.board.grid.each_slice(10) do |slice|%>
			<tr>
			<% slice.each do |key, v| %>
				<td>
					<%= v.status %>
			</td>
		<%end%>
		</tr>
	<%end%> 
</table>