<title>Place Boats Page</title>

Let's start the game <%=@player.name%>.
 
You have <%=@player.ships_to_deploy.count%> ships to deploy. 
<%=ship = @player.ships_to_deploy.last %>
Where would you like to deploy <%=ship.class%>?

<form action='/place_boat_part' method="post">

	<input type="hidden" name = 'ship' value= "<%=ship.class%>">

	<table>
		<% @player.board.grid.each_slice(10) do |slice|%>
			<tr>
			<% slice.each do |key, v| %>
				<td>
				<% if v.content.class == Water %>
					<input type="submit" name = "cell_key" value = "<%= key %>">
				<% else %>
					<%= v.content.class %>
				<% end %>
				</td>
					</form>
				<%end%> 
			</tr>
		<%end%> 
	</table>

	Please choose the direction you would like to deploy <%=ship.class%>.
	<select name='orientation'>
		<option value="horizontal">Horizontal</option>
		<option value="vertical">Vertical</option>
	</select>	
</form>
